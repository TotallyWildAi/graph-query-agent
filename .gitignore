# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Project preview/system artifacts
.thumbnail

# Design-iteration screenshots (verification artifacts, not deliverables)
screenshots/

# Reference captures — keep STYLE.md + tokens.json in git, not the heavy PNGs
design-reference/**/*.png
